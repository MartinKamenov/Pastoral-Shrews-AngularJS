export class IComment {

    constructor(userEmail: string, text: string, date: Date) {
        this.userEmail = userEmail;
        this.text = text;
        this.date = date;
    }
       userEmail: string;
       text: string;

       date: Date;
    }
